@model ProyectoTFG_League.Models.CampeonModelo

@{
    ViewData["Title"] = "Eliminar Campeon";
}

<h3 class="text-center">¿Estás Seguro de que quieres Eliminar este Campeón?</h3>
<br />
<div>
    <hr />
    <dl class="row">
        <dd class="col-sm-10">
            @if (Model.Imagen != null)
            {
                <img src="data:image;base64,@(Convert.ToBase64String(Model.Imagen))" alt="Imagen" width="60" height="60" />
            }
        </dd>
        <dd class="col-sm-10">
            <span class="font-weight-bold h4">@Html.DisplayFor(model => model.Nombre)</span>
        </dd>
    </dl>

    <form asp-action="Delete" method="post">
        <input type="hidden" asp-for="ID" />
        <div class="form-group">
            <input type="submit" value="Eliminar" class="btn btn-danger" /> |
            <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
        </div>
    </form>
</div>
